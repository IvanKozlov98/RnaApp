syntax = "proto3";

message Null {}

message HumanGenetic {
  string DNA = 1;
  string label = 2;
}

message Health {
  string health = 1;
}

service TestService {
  rpc GetHealth(HumanGenetic) returns (Health);
}
